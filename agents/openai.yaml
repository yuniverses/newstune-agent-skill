interface:
  display_name: "NewsTune Agent API"
  short_description: "Use NewsTune's API key based Agent API."
  default_prompt: "Use $newstune-agent-api to validate my NewsTune API key, confirm the podcast setup, create content only after confirmation, wait for episode completion, and give me the direct episode link."
