interface:
  display_name: "NewsTune Agent API"
  short_description: "Use NewsTune's API key based Agent API."
  default_prompt: "Use $newstune-agent-api to validate my cached or newly provided NewsTune API key, interview me about the podcast/content system I want, confirm sources/hosts/voices/generation mode, then create private content only after confirmation."
