interface:
  display_name: "NewsTune Agent API"
  short_description: "Create, schedule, and publish podcasts with the Agent API."
  default_prompt: "Use $newstune-agent-api to validate my cached NewsTune API key, interview me about sources, hosts, voices, generation mode, and publishing, then create content after confirmation. For a recurring schedule, confirm one detailed execution contract and let covered runs generate and publish automatically without asking again."
